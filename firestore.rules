rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Basic rules for a single-user app without authentication
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
